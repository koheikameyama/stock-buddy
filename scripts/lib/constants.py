"""
Python スクリプト共通定数

TypeScript側の lib/constants.ts と対応する値にはコメントで明記。
値を変更する場合は、対応するTS側の定数も確認すること。
"""

# =============================================================================
# OpenAI設定
# =============================================================================

OPENAI_MODEL = "gpt-4o-mini"
OPENAI_TEMPERATURE = 0.3
OPENAI_MAX_TOKENS_INSIGHT = 100
OPENAI_MAX_TOKENS_IMPROVEMENT = 200

# AI API同時リクエスト数の制限
AI_CONCURRENCY_LIMIT = 3

# =============================================================================
# 株価データ取得（※TS側 lib/constants.ts と同じ値）
# =============================================================================

# 株価取得失敗の警告閾値（データ取得不可の可能性）
# → TS: FETCH_FAIL_WARNING_THRESHOLD
FETCH_FAIL_WARNING_THRESHOLD = 3

# 株価データの鮮度チェック（日数）: これより古いデータは無視
# → TS: STALE_DATA_DAYS
STALE_DATA_DAYS = 14

# チャートデータに必要な最小データポイント数
# この数未満のデータしかない銘柄は hasChartData = false にする
# → TS: MIN_CHART_DATA_POINTS
MIN_CHART_DATA_POINTS = 20

# =============================================================================
# バッチ処理
# =============================================================================

# yf.download() 1回あたりの最大銘柄数
DOWNLOAD_BATCH_SIZE = 100

# DB更新のバッチサイズ
DB_BATCH_SIZE = 100

# =============================================================================
# yfinance レート制限対策
# =============================================================================

# レート制限エラー時のリトライ回数
YFINANCE_RATE_LIMIT_MAX_RETRIES = 5

# リトライ間隔（秒）: 指数バックオフ
YFINANCE_RATE_LIMIT_WAIT_SECONDS = [30, 60, 120, 240, 480]

# バッチ間のスリープ時間（秒）: レート制限を避けるため
YFINANCE_BATCH_SLEEP_SECONDS = 1

# =============================================================================
# 業績データ取得
# =============================================================================

# 全銘柄を取得するのに何日かけるか
EARNINGS_ROTATION_DAYS = 7

# リクエスト間隔（秒）
EARNINGS_SLEEP_INTERVAL = 0.5

# =============================================================================
# データ保持・評価
# =============================================================================

# 古いデータの保持期間（日数）
RETENTION_DAYS = 30

# =============================================================================
# 価格アラート
# =============================================================================

# 急騰しきい値（%）
SURGE_THRESHOLD = 5.0

# 急落しきい値（%）
PLUNGE_THRESHOLD = -5.0

# =============================================================================
# ATRベース出口戦略
# =============================================================================

# 利確マイルストーン（%）: この利益率に達したら通知
# → TS: ATR_EXIT_STRATEGY.PROFIT_MILESTONES
PROFIT_MILESTONES = [10, 20, 30]

# =============================================================================
# 日次上昇/下落ランキング
# =============================================================================

# 上昇/下落それぞれの銘柄数
MOVERS_COUNT = 5

# =============================================================================
# OpenAI使用量チェック
# =============================================================================

# 為替レート（円/ドル）
USD_TO_JPY = 150

# gpt-4o-mini料金（USD per 1M tokens）
GPT4O_MINI_PRICING = {"input": 0.15, "output": 0.60}

# =============================================================================
# ユーザーアクティビティ
# =============================================================================

# バッチ処理の非アクティブ除外閾値（日数）
# → TS: USER_ACTIVITY.INACTIVE_THRESHOLD_DAYS
INACTIVE_THRESHOLD_DAYS = 7

# =============================================================================
# ギャップ予測（プレマーケットデータ取得）
# =============================================================================

# yfinanceティッカー
# プレマーケットデータ取得スケジュール（cron-job.org）:
# - 06:00 JST: 大証ナイトセッション終了後のデータ取得
# - 08:00 JST: プレモーニングセッション（session-batch経由）
PRE_MARKET_TICKERS = {
    "nikkei_futures": "NKD=F",
    "usdjpy": "USDJPY=X",
    "sp500": "^GSPC",
    "nasdaq": "^IXIC",
    "vix": "^VIX",
    "wti": "CL=F",
}

# 取得期間（直近N日）
PRE_MARKET_FETCH_DAYS = 5
