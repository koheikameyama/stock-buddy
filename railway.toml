[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# startCommandを削除してDockerfileのCMDを使用
# DockerfileのCMD: npx prisma migrate deploy && npm start
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
