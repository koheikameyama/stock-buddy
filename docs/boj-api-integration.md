# 日銀統計API（BOJ Time-Series Data Search API）統合計画

## 概要

2026年2月18日より、日本銀行の時系列統計データ検索サイトがAPIを正式公開しました。
Stock Buddy では、このAPIを活用してマクロ経済環境の分析精度を高め、より質の高い銘柄推薦を実現します。

**公式ドキュメント**: https://www.stat-search.boj.or.jp/
**APIマニュアル**: https://www.stat-search.boj.or.jp/info/api_manual.pdf

---

## API仕様

### エンドポイント

| 種別 | URL |
|------|-----|
| コードAPI（系列コード指定） | `GET https://www.stat-search.boj.or.jp/api/v1/getDataCode` |
| 階層API（DB階層指定） | `GET https://www.stat-search.boj.or.jp/api/v1/getDataLayer` |
| メタデータAPI | `GET https://www.stat-search.boj.or.jp/api/v1/getMetadata` |

### 主要パラメータ

| パラメータ | 必須 | 説明 | 例 |
|-----------|------|------|-----|
| `code` | ○ | 系列コード（DB名を含まない） | `STRDCLUCON` |
| `db` | - | DB名（階層APIで使用） | `FM01` |
| `startDate` | - | 開始期（月次: YYYYMM） | `202001` |
| `endDate` | - | 終了期（月次: YYYYMM） | `202601` |
| `format` | - | 出力形式 | `json` or `csv` |
| `lang` | - | 言語 | `jp` or `en` |

### レスポンス形式（JSON）

```json
{
  "STATUS": 200,
  "MESSAGE": "正常に終了しました。",
  "RESULTSET": {
    "SERIES_CODE": "STRDCLUCON",
    "NAME_OF_TIME_SERIES_J": "無担保コールO/N物（加重平均）",
    "VALUES": {
      "SURVEY_DATES": [202501, 202502],
      "VALUES": [0.232, 0.238]
    }
  }
}
```

---

## 取得可能な主要統計

### 金利関連

| DB名 | 統計名 | 主な系列コード | 期種 |
|------|--------|---------------|------|
| `FM01` | 無担保コールO/N物レート | `STRDCLUCON`（加重平均） | 日次 |
| `FM02` | 短期金融市場金利 | コールレート、レポレートなど | 月次 |
| `IR01` | 基準割引率・基準貸付利率（政策金利） | `MADR1Z@D` | 日次 |
| `IR02` | 預金種類別店頭金利 | 普通預金、定期預金など | 月次 |

### マネー関連

| DB名 | 統計名 | 説明 | 期種 |
|------|--------|------|------|
| `MD01` | マネタリーベース | 日銀が供給するお金の量 | 月次 |
| `MD02` | マネーストック | 市中に流通するM1/M2/M3 | 月次 |
| `MD06` | 日銀当座預金増減要因と金融調節 | 金融緩和の状況 | 日次 |

### 為替・市場

| DB名 | 統計名 | 説明 | 期種 |
|------|--------|------|------|
| `FM` 系 | 外国為替相場 | 円ドル・円ユーロなど | 日次 |
| `CO` 系 | コーポレートボンド | 社債市場データ | 月次 |

### その他（ファンダメンタルズ）

| DB名 | 統計名 | 説明 | 期種 |
|------|--------|------|------|
| `PR` 系 | 国内企業物価指数（PPI） | 企業の仕入れコスト | 月次 |
| `CPI` 系 | 消費者物価指数（CPI） | 実質購買力・インフレ | 月次 |
| `BS` 系 | 日銀バランスシート | 日銀資産規模 | 月次 |

---

## Stock Buddy での活用案

### ユースケース 1: マクロ環境バナー（フロント表示）

**目的**: 投資判断に影響する金融環境をユーザーにひと目で伝える

```
┌─────────────────────────────────────────────────────────┐
│ 📊 現在の金融環境                                         │
│                                                         │
│  政策金利: 0.25%  ↑（前月比 +0.1%）                     │
│  マネーストック(M2): +2.8% YoY  [やや拡張的]             │
│  円相場: ¥149.2/USD                                      │
│                                                         │
│  💡 金利上昇局面は銀行・保険セクターに追い風です         │
└─────────────────────────────────────────────────────────┘
```

**取得データ**:
- `FM01`: 無担保コールO/N物レート（政策金利の代理指標）
- `MD02`: マネーストックM2（前年比）
- `FM` 系: 円ドル為替レート

---

### ユースケース 2: セクター分析への金利環境の組み込み

**目的**: 金利水準に基づいてセクターの有利・不利をルールベースで判定し、AI推薦の根拠に追加する

**金利とセクターの関係**:

| 金利環境 | 有利なセクター | 不利なセクター |
|----------|--------------|---------------|
| 金利上昇局面（利上げ） | 銀行・保険・証券 | 不動産・電力・公益 |
| 金利低下局面（利下げ） | 不動産・グロース株 | 銀行・保険 |
| 金利横ばい（現状維持） | 個別銘柄のファンダメンタルズ重視 | - |

**実装イメージ（`scripts/github-actions/fetch_macro_indicators.py`）**:
```python
import requests

BOJ_API_BASE = "https://www.stat-search.boj.or.jp/api/v1/getDataCode"

def fetch_policy_rate() -> float | None:
    """無担保コールO/N物レート（直近値）を取得"""
    response = requests.get(BOJ_API_BASE, params={
        "code": "STRDCLUCON",
        "format": "json",
        "lang": "jp",
    })
    data = response.json()
    values = data["RESULTSET"]["VALUES"]["VALUES"]
    return values[-1] if values else None

def classify_rate_environment(rate: float) -> str:
    """金利水準から相場環境を分類"""
    if rate < 0.1:
        return "超低金利"
    elif rate < 0.5:
        return "低金利"
    elif rate < 1.0:
        return "中立"
    else:
        return "高金利"
```

---

### ユースケース 3: AI推薦プロンプトへのマクロ文脈追加

**目的**: OpenAI に渡す銘柄分析プロンプトにマクロ環境を含めることで推薦精度を向上させる

**現在のプロンプト**（銘柄ファンダメンタルズのみ）:
```
銘柄: トヨタ自動車 (7203.T)
PER: 9.2, PBR: 1.1, ROE: 12.3%
...
```

**改善後のプロンプト**（マクロ環境を追加）:
```
【現在のマクロ環境】
- 政策金利: 0.25%（前月比 +0.1% 上昇局面）
- マネーストック(M2): +2.8% YoY（緩やかな拡張）
- 円ドル: 149円（円安傾向）

【銘柄情報】
銘柄: トヨタ自動車 (7203.T)
PER: 9.2, PBR: 1.1, ROE: 12.3%
...
```

---

### ユースケース 4: 「マクロ環境レポート」機能

**目的**: 週次レポートにマクロ環境のサマリーを追加し、ユーザーの学習を促進する

```
📰 今週のマクロ環境サマリー（2026-02-17 〜 2026-02-21）

🏦 金融政策
  政策金利: 0.25%（据え置き）
  日銀コメント: 「経済・物価の見通しが実現すれば引き続き利上げ方針」

💰 マネーサプライ
  M2: +2.8% YoY → 市場への資金供給は穏やか

💴 為替
  円ドル: ¥149.2（前週比 -0.8円、円高方向）

📈 株式への影響
  円高傾向は輸出株に逆風。内需・金融セクターに注目。
```

---

## 実装計画

### フェーズ1: データ取得スクリプト（優先度: 高）

**新規ファイル**: `scripts/github-actions/fetch_macro_indicators.py`

```
取得データ:
- 無担保コールO/N物レート (FM01)
- マネーストック M2 前年比 (MD02)
- マネタリーベース (MD01)

実行タイミング:
- GitHub Actions: 月次（月初の第1営業日）
- 更新頻度が月次のデータが多いため

保存先:
- 新テーブル: MacroIndicator
  - date: Date（JST基準）
  - indicator: String（指標名）
  - value: Decimal
  - unit: String（%、兆円など）
  - source: String（"BOJ"）
```

### フェーズ2: DBスキーマ追加

```prisma
model MacroIndicator {
  id        Int      @id @default(autoincrement())
  date      DateTime @db.Date
  indicator String   // "policy_rate", "money_stock_m2_yoy", etc.
  value     Decimal  @db.Decimal(10, 4)
  unit      String   // "%", "兆円", "億円"
  source    String   @default("BOJ")
  createdAt DateTime @default(now())

  @@unique([date, indicator])
  @@index([date])
  @@index([indicator])
}
```

### フェーズ3: APIエンドポイント追加

```
GET /api/macro/latest
→ 最新のマクロ指標一覧を返す

GET /api/macro/history?indicator=policy_rate&months=12
→ 指定指標の過去12ヶ月分を返す
```

### フェーズ4: フロントエンド表示

```
対象ページ:
- トップページ: マクロ環境バナー（コンパクト表示）
- 銘柄詳細ページ: 金利影響の補足説明
- AIレポートページ: マクロ環境サマリー
```

---

## 実装上の注意点

### APIの制限事項

- 1リクエストで最大200系列コードまで指定可能
- 検索ヒット数の上限は1,250系列
- 大量取得時は `NEXTPOSITION` を使ってページネーション
- レート制限の明示的な記載なし → 念のため1秒以上のウェイトを挟む

### 系列コードの調べ方

1. [時系列統計データ検索サイト](https://www.stat-search.boj.or.jp/)にアクセス
2. 「統計別検索」または「系列名称検索」で目的のデータを検索
3. 系列詳細画面でデータコード（例: `FM01'STRDCLUCON`）を確認
4. APIパラメータには**DB名なし**の部分（`STRDCLUCON`）のみを指定

### 注意: APIはDB名なしで系列コードを指定

```python
# ✅ 正しい: DB名なし
params = {"code": "STRDCLUCON"}

# ❌ エラーになる: DB名あり
params = {"code": "FM01'STRDCLUCON"}
```

### データ更新タイミング

| 統計 | 公表タイミング |
|------|--------------|
| 無担保コールレート | 毎営業日 |
| マネーストック | 毎月中旬（速報） |
| マネタリーベース | 毎月第1営業日 |
| 基準割引率 | 金融政策決定会合後 |

---

## 参考リンク

- [日銀 API 機能提供開始のお知らせ](https://www.boj.or.jp/statistics/outline/notice_2026/not260218a.htm)
- [時系列統計データ検索サイト](https://www.stat-search.boj.or.jp/)
- [APIマニュアル（日本語PDF）](https://www.stat-search.boj.or.jp/info/api_manual.pdf)
- [APIマニュアル（英語PDF）](https://www.stat-search.boj.or.jp/info/api_manual_en.pdf)
- [主要時系列統計データ一覧](https://www.stat-search.boj.or.jp/info/nme_Mdframe.html)
