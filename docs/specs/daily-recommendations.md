# 日次おすすめ 仕様書

## 概要

ユーザーごとに1日最大5銘柄をAIが選定する、Stock Buddyの中核機能です。

## 生成タイミング

取引日の3セッション（朝9時 / 昼12:30 / 夕15:35 JST）

## 生成フロー

1. 全ユーザーの投資設定を取得
2. ユーザーごとに並列処理（最大3並列）:
   a. 残り予算を計算（保有コスト方式: 売却済み株は含まない）
   b. 対象銘柄を取得（最新株価データあり、上場廃止・データ取得失敗を除外）
   c. 緩い予算フィルタ（残り予算を10万円単位で切り上げた値以内）で候補を広めに取得
   d. 買い候補フィルタ（チャート分析 + ファンダメンタル分析で買い推奨できる銘柄のみ通過）
   e. スコアリング（投資スタイル別重み + 戦略適合性ボーナス）
   f. セクター分散フィルタ（セクタートレンド連動: 強い追い風7/追い風6/中立5/逆風3/強い逆風2銘柄）
   ※ 保有銘柄・ウォッチリスト銘柄は除外しない（候補として残す）
   g. 予算内の銘柄を優先して絞り込み（5件未満なら予算超の銘柄も安い順に追加）
   h. 上位15銘柄をOpenAIに送信（予算超過時はその旨をプロンプトに含める）
   i. AIが7銘柄を選定 + 理由生成
   j. セーフティフィルタで除外
   k. 上位5銘柄を保存
3. `UserDailyRecommendation` テーブルに保存

## 買い候補フィルタ

スコアリング前にルールベースでチャート分析・ファンダメンタル分析の観点から「買い推奨できる」銘柄のみに絞り込む。

### チャート分析フィルタ

以下すべてを満たす銘柄が通過:

1. **過熱でない**: MA乖離率 < 15%（AGGRESSIVEはスキップ）
2. **下落トレンドでない**: 週間変化率 > スタイル別閾値（CONSERVATIVE: -10% / BALANCED: -15% / AGGRESSIVE: -20%）
3. **極端な急騰でない**: 週間変化率 < スタイル別閾値（CONSERVATIVE: 20% / BALANCED: 25% / AGGRESSIVE: 50%）
4. **少なくとも1つのポジティブシグナル**:
   - 週間変化率 > 0%（正のモメンタム）
   - OR MA乖離率 < -5%（押し目買い圏）
   - OR 出来高比率 > 1.5（注目度高）

### ファンダメンタル分析フィルタ（投資スタイル別）

- **CONSERVATIVE**: 黒字必須 + 業績悪化中（profitTrend: decreasing）でない
- **BALANCED**: 赤字でない（不明はOK）+ 業績悪化かつ減収でない
- **AGGRESSIVE**: 赤字×高ボラティリティ（50%超）のみ除外

定数: `lib/constants.ts` の `BUY_CANDIDATE_FILTER`
ロジック: `lib/recommendation-buy-filter.ts`

## スコアリング

おすすめ銘柄のスコアリングは「今買い時か」ではなく「ユーザーの投資戦略に合っているか」のみで判断する。購入タイミングの判断は別機能（[購入判断](purchase-analysis.md)）が担当する。

スコアリングに含まれる要素:
- 投資スタイル別の重み（モメンタム、出来高、ボラティリティ、時価総額）
- 赤字+高ボラティリティのリスクペナルティ（スタイル別）
- 配当・PBR・PER・成長性・ROE等のスタイル別ボーナス
- セクタートレンド連続ボーナス（compositeScore × 0.25、デッドゾーンなし）
- セクター順位ボーナス（compositeScore降順で上位3セクターに加点、下位3セクターに減点）

## 2段階の予算フィルタ

1. **緩いフィルタ**（スコアリング前）: 残り予算を10万円単位で切り上げた値以内の銘柄を候補に含める（例: 残り3万→10万、残り12万→20万）
2. **最終絞り込み**（スコアリング後）: 予算内の銘柄を優先。5件未満なら予算超の銘柄も安い順に追加し、AIプロンプトに予算超過の旨を含める

## 投資テーマ

- `短期成長` - 直近の上昇モメンタムが強い銘柄
- `中長期安定成長` - 安定した業績と成長性
- `高配当` - 高い配当利回り
- `割安反発` - PBR/PERが低く反発期待
- `テクニカル好転` - テクニカル指標が買いシグナル
- `安定ディフェンシブ` - 低ボラティリティで守備的

## AI出力スキーマ

```json
{
  "marketSignal": "bullish | neutral | bearish",
  "selections": [
    {
      "tickerCode": "銘柄コード",
      "reason": "おすすめ理由（投資戦略への適合根拠）",
      "investmentTheme": "短期成長 | 中長期安定成長 | 高配当 | 割安反発 | テクニカル好転 | 安定ディフェンシブ"
    }
  ]
}
```

## 保有銘柄・ウォッチリスト銘柄の推奨

保有銘柄・ウォッチリスト銘柄はおすすめ候補から除外せず、条件が合えば積極的に推奨する。

- 保有銘柄が候補に含まれる場合、AIプロンプトに保有情報を伝え「戦略適合」の文脈で理由を生成
  - AIは reason の冒頭に「【保有中の注目銘柄】」を付与
  - フロントエンドでは「買い増しチャンス」バッジを表示（青色）
- ウォッチリスト銘柄が候補に含まれる場合、AIプロンプトにウォッチリスト情報を伝え「戦略適合」の文脈で理由を生成
  - AIは reason の冒頭に「【注目していた銘柄】」を付与
  - フロントエンドでは「買い時かも」バッジを表示（緑色）

## セッション別のプロンプト

| セッション | フォーカス | 時間軸 |
|-----------|----------|--------|
| morning | 今日の前場に注目したい銘柄 | 今日〜今週 |
| afternoon | 後場に注目したい銘柄 | 今日の後場〜明日 |
| evening | 明日以降に仕込みたい銘柄 | 明日〜来週 |

## プロンプトの評価基準

- スタイル適合理由: `reason` に「なぜこのスタイルのユーザーに適しているか」を含める
- セクター偏り制約: 同一セクター最大2銘柄、バリュー/グロース比率をスタイル別に調整

## 投資スタイル別の選定基準

| 基準 | 保守的 | バランス | 積極的 |
|------|--------|----------|--------|
| 時価総額 | 大型中心 | 混合 | 小〜中型含む |
| ボラティリティ | 低 | 中 | 高も許容 |
| 黒字必須 | 必須 | 基本的に黒字 | 赤字も許容 |
| モメンタム重視 | 低 | 中 | 高 |

## セーフティフィルタ（AI選定後の除外）

AI選定後、購入分析の強制補正ルールと共通のセーフティチェックを適用し、購入分析で見送り推奨になる銘柄を事前に除外する。

| # | ルール | 条件 | 共通元 |
|---|--------|------|--------|
| 1 | 危険銘柄 | 赤字 + ボラティリティ > 50% | 購入判断ルール4 |
| 2 | 赤字×急騰 | 赤字企業が週間+20%以上急騰 | 購入判断ルール5 |
| 3 | 決算直前 | 決算発表まで3日以内 | 購入判断の決算直前ブロック |
| 4 | 極端な急騰 | 週間変化率 ≥ 50% | スタイル補正の極端急騰ブロック |
| 5 | 極端な過熱 | MA乖離率 ≥ 50% | スタイル補正の極端過熱ブロック |
| 6 | 下落トレンド | 週間変化率が投資スタイル別閾値以下（安定配当-10%/成長-15%/アクティブ-20%） | スタイル補正の下落トレンドロック |

判定ロジックは `lib/stock-safety-rules.ts` の `checkRecommendationSafety()` に集約。

## 手動再生成

ダッシュボードの「再生成」ボタンからユーザーが手動でおすすめを再生成できる。内部的に `generate-daily` を単一ユーザーで呼び出す。既存の当日おすすめを削除してから再生成するため、銘柄数が減っても古いデータが残らない。

## AI設定

| モデル | Temperature | 出力形式 |
|--------|-------------|----------|
| GPT-4o-mini | 0.4 | JSON Schema |

## プロンプト設計原則

- **ハルシネーション防止**: 提供されたニュース以外を引用しない明示的指示
- **初心者向け言語**: 専門用語 + 必ず解説を付与
- **エビデンス必須**: すべての判断に根拠を引用
- **安全優先**: AI予測よりも安全ルールを優先
- **パーソナライズ**: ユーザーの投資スタイル・予算に合わせた文脈

## API

| エンドポイント | 認証 | 説明 |
|---------------|------|------|
| `POST /api/recommendations/generate-daily` | CRON_SECRET | 日次おすすめ生成 |
| `POST /api/recommendations/regenerate` | セッション認証 | 手動再生成 |

## 関連ファイル

- `app/api/recommendations/generate-daily/route.ts` - 日次おすすめ生成
- `app/api/recommendations/regenerate/route.ts` - おすすめ手動再生成
- `app/api/featured-stocks/route.ts` - おすすめ銘柄取得
- `lib/recommendation-scoring.ts` - スコアリング
- `lib/stock-safety-rules.ts` - 安全ルール（おすすめ・購入共通）
- `lib/prompts/daily-recommendation-prompt.ts` - 日次おすすめプロンプト
